{{signin-header}}

<form {{action "signIn"}} id="signin_form">
	<div>{{input type="email" placeholder="email" value=submittedEmail}}</div>
	<div>{{input type="password" placeholder="password" value=submittedPassword}}</div>
	{{!-- <div><button {{action "signIn" "email"}}>Sign In</button></div> --}}
	<div>{{input type="submit" value="Sign In"}}</div>
</form>